<!DOCTYPE html>
<html lang="en">

<head>
  <link rel="stylesheet" href="../public//MainStyle.css">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%if(title!=null){%>
  <title><%=title%> Quiz</title>
  <%}else{%>
  <title>Quiz</title>
  <%}%>
  
  <link rel="shortcut icon" type="image/x-icon" href="../public/MainPage.html">
</head>

<body>
  <header>
    <%if(title!=null){%>
  <div class="od"><a id="club" href="/"
      style="color: dodgerblue;font-style: oblique;font-weight: bold;font-size: 45px;"><%=title%></a><br />
    <%}else{%>
    <div class="od"><a id="club" href="/"
        style="color: dodgerblue;font-style: oblique;font-weight: bold;font-size: 45px;">Quiz</a><br />
      <%}%>
  

      <ul>

        <% if(!currentUser){ %>
      <li><a href="/login">Sign in</a></li>
      <li><a href="/register">Sign up</a></li>
      <%}else{%>}
      <li><a href="#">Signed in as <%=currentUser.username%></a></li>
      <li><a href="/logout">Logout</a></li>
      <%}%>
        <li style="margin-right: 0px;"><a href="/quiz/leaderboard" style="margin-right: 0px;">LeaderBoard</a></li>
      </ul>
    </div>
    </div>
  </header>
  <div class="Maincontent">
    <%if(title!=null && tag!=null){%>
      <h1><%=title%></h1>
      <p class="subtext"><%=tag%></p>
      <a type="button" href="/quiz" class="button" value="Input Button">Start now</a>
      <div>
        <%}else{%>
        <h1>Quiz</h1>
        <p class="subtext">Fancy tag line!</p>
        <a type="button" href="/quiz" class="button" value="Input Button">Start now</a>
        <div>
          <%}%>
    
        <script>
          window.onload = function () {
            if (!window.location.hash) {
              window.location = window.location + '#loaded';
              window.location.reload();
            }
          }
        </script>
   <%- include('./partials/footer') %>